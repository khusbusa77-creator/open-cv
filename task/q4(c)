import cv2

img = cv2.imread("image.jpg")

b,g,r = cv2.split(img)

zero = g*0

merged = cv2.merge([b, zero, r])

cv2.imshow("Blue", b)
cv2.imshow("Green", g)
cv2.imshow("Red", r)
cv2.imshow("Merged", merged)

cv2.imwrite("merged.jpg", merged)

cv2.waitKey(0)
cv2.destroyAllWindows()
